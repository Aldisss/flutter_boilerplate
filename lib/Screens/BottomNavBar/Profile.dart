import 'package:flutter/material.dart';
import 'package:flutter_boilerplate/Shared/shared.dart';

class ProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return InitialView(
      title: "Profile",
    );
  }
}
